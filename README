<h1>Image Playground</h1>
This project aims to convert PDF to image and trim whitespaces of images.  
Currently, this project serves the functionalities only and there is no design implemented in the webpages.  

To run, please ensure you have Python 3.9 or above. Run the commands below in terminal:  
```
pip install -r requirements.txt
flask run
```